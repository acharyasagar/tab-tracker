# Tab tracker back-end

> 

## Build Setup

``` bash
# install dependencies
npm install

```

### Create a database 'tabtracker' before starting the application.

``` bash
# after creating database, to start the server
npm start

```
